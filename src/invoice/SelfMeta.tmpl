import Principal "mo:base/Principal"
module {
    public type Meta = {
        canisterId : Principal;
    };
    public func getMeta () : Meta {
        return {
            canisterId: Principal = Principal.fromText("${CANISTER_ID}");
        };
    };
}
